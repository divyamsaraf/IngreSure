__pycache__/
backend/venv/
PROJECT_DETAILS.md

# Documentation (phase reports, technical/refactor/arch/tech stack)
docs/
backend/docs/

# Local-only scripts (verification, restart; not required in repo)
backend/scripts/verify_chat_audit.py
backend/scripts/restart_backend.sh

# Local env (flags, secrets)
backend/.env
backend/.commit_msg
frontend/.env
frontend/.env.local
frontend/.env.*.local

# User-specific / runtime data (do not commit secrets or env-specific learned data)
data/profiles.json
data/unknown_ingredients_log.json
data/learned_regional_mappings.json

# Root env for Compose variable substitution (optional)
/.env

# Local deployment / phase write-ups (keep out of remote)
DEPLOY.md
OCI_STEP_BY_STEP.md
PHASES.md
