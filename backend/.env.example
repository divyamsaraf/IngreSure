# IngreSure backend env (copy to .env)
# Use new deterministic compliance engine for /scan and /chat/grocery
USE_NEW_ENGINE=false
# When true, run legacy engine alongside and log diffs (for comparison)
SHADOW_MODE=false
